version: 2

models:
  - name: customers_sample
    columns:
      - name: CustomerID      
        tests:
          - unique
          - not_null
      - name: CustomerName
        tests:
          - not_null
      - name : Segment
        tests:
          - accepted_values:
              values:
                - 'Consumer'
                - 'Corporate'
                - 'Home Office'
        
              
              